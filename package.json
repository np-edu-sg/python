{
  "name": "python",
  "version": "0.1.0",
  "description": "Python crash course",
  "scripts": {
    "build": "rimraf dist/ && vitepress build && pnpm -r --parallel run build"
  },
  "keywords": [
    "python"
  ],
  "author": "Qin Guan",
  "license": "CC-BY-SA-4.0",
  "devDependencies": {
    "rimraf": "^5.0.1",
    "vitepress": "1.0.0-rc.11",
    "vue": "^3.3.4"
  }
}
